# Packages

Shared implementation packages will live here after the app boundaries settle.

Current shared code still lives in `src/` to avoid breaking existing imports. Future candidates:

- media/provider contracts;
- shared FFmpeg helpers;
- reusable audio/TTS workflow;
- shared project/job manifests;
- renderer utilities.

