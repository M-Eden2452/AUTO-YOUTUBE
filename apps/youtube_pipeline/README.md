# YouTube Pipeline App

Wrapper around the existing root `pipeline.py` application.

The implementation has not been moved yet. This preserves backwards compatibility with existing commands while giving the project a future app boundary.

