# Anime Factory App

Wrapper around the existing `anime_factory/pipeline.py` application.

The implementation remains in place for now because the current files and tests are already working. This wrapper marks the future app boundary.

