# FIRST OWNER SHORT — owner diagnostic, 2026-08-13

Диагностический owner-прогон на clean HEAD `92520ee` (CI `offline-tests`
run 31700150121 — `success`). Код не менялся, PLAN не двигался, M1-E не
начинался. Оба прогона read-only по отношению к маршруту.

**Итог одной строкой:** ни LOCAL, ни STOCK путь не довёл ролик до
`draft_1080x1920.mp4`. Оба остановились на одном и том же структурном месте —
`assembly_has_no_slots` — придя к нему с разных сторон. Платных вызовов не
выполнено ни одного (`paid_call_performed: false`).

Разрешения владельца на прогон: сеть `provider_search` + `asset_download` — ON,
ElevenLabs — ON (один attempt), Vision — OFF, paid semantic — OFF.
Фактически израсходовано: **ничего**. Сеть разрешена и ни разу не использована.

---

## RUN 1 — LOCAL ASSETS · остановлен, BLOCKER

Проект: `2026-08-13_polog-dozhdevogo-lesa` (канал `nature_science_news_ru`,
шаблон `fullscreen_voiceover_v1`, `draft_complete`, 5 сцен, 24.5 с).

Отбор материала выполнен по правилам: медиатека `assets/library/videos`
(63 файла) найдена, 24 клипа просмотрены кадрами, вертикальный кроп 9:16
проверен реальным ffmpeg-кропом, сценарий написан **после** просмотра строго
под существующие кадры, авторский `--visual-brief` составлен на 5 сцен.

### BLOCKER-L1 · У локального пути нет входа в канонический CLI

Три независимо проверенных факта:

1. `create` для `fullscreen_voiceover_v1` **никогда не передаёт** `user_assets`:
   вызов `create_news_to_short_job` в
   `src/ai_youtube/apps/content_creator/workflows/fullscreen_voiceover/use_case.py:117`
   не содержит этого аргумента. Инфраструктура под него при этом живая —
   `NewsJob.user_assets` хранится (`src/news/models.py:194`), планировщик
   выставляет `preferred_asset_ids=[user_asset_NNN]`
   (`src/content/visual_planning/legacy_format.py:292`), билдер ранжирует их
   первыми (`rank_user_assets`). CLI-флага нет; `--source-asset` парсится в
   request, но на fullscreen-пути не читается ни разу — он только для
   `story_card_text_only_v1`.
2. `assets replace` умеет только заменять **существующий** слот. Фактическая
   ошибка: `Visual slot 'slot_001' was not found in scene 'scene_001';
   available slots: none.`
3. Слоты появляются только после успешной сборки. Замкнутый круг: ручная
   подстановка требует, чтобы автоматический этап сначала отработал.

### BLOCKER-L2 · Вся локальная библиотека нерендерабельна по правам

Библиотека не молчала: 5 кандидатов на сцену, все `provider: local_library`,
все отклонены с `rights_status: review_required`.

Причина в данных: ни у одного элемента `assets/library/metadata/media_index.json`
нет поля `rights_status`, срабатывает дефолт `reference_only`, а к рендеру
допускаются только `user_owned / creative_commons / licensed / public_domain`
(`src/assets/models.py`).

**Гейт отработал правильно.** Это дефект данных библиотеки, а не гейта.
По решению владельца `media_index.json` в этом прогоне не правился.

### Прочие находки RUN 1

| Находка | Класс |
|---|---|
| Локальный ранжировщик слеп к содержанию при русском запросе: 10 матчей, у всех score 3 = `type`+`aspect_16_9`+`duration`, keyword-хитов ноль. Топ-1 для «полога дождевого леса» — `macro_insects`, топ-2 — `lightning_storm_airplane` (`_score_asset`, `src/media_library.py:528`) | later debt |
| Метаданные библиотеки описывают поисковый запрос, а не содержимое кадра: файл `..._storm_clouds_..._rainforest_..._36936635.mp4` показывает терминал аэропорта. В том же кластере — авиалайнер, торнадо над пшеничным полем, заснеженные горы, шторм над океаном | later debt |
| Импорт нового внешнего файла требует `--confirm-user-owned`; `--source-url` и `--license-file` гейт сами по себе не проходят (`src/assets/completion/replacement.py:150`). Для легально лицензированного стока, которым владелец не владеет, правдивого варианта в CLI нет | later debt |
| `asset_search` рапортует `completed`, отдав ноль пригодного материала | UX inconvenience |
| Пять `preview.part.mp4` нулевого размера при неразрешённой сети | UX inconvenience |
| Подсказка `rerun` предлагает `--voice-provider disabled` — скопировавший её получит немой ролик, хотя канал настроен на `elevenlabs` | UX inconvenience |
| **Положительное:** авторский `--visual-brief` доехал до сцен полностью — `primary_query` собран из `subject`/`action`/`place`, `must_not_include` донёс запреты. Обратная сторона **C63** подтверждена: на user-supplied пути бриф не теряется | expected behaviour |

---

## RUN 2 — STOCK RETRIEVAL · остановлен, BLOCKER

Проект: `2026-08-13_pchela-i-odin-cvetok`. Тема — пчёлы и опыление, выбрана как
плотно покрытая стоком. Visual brief **намеренно не задавался**: цель — проверить
автоматический путь, а ручные английские `provider_queries` подменили бы его.

### BLOCKER-S1 · Ни один внешний провайдер не был опрошен

40 provider attempts, **ноль отправленных запросов**:

- 25 попыток (5 сцен × `pexels`, `pixabay`, `wikimedia`, `internet_archive`,
  `nasa_images`) — `status: skipped`, `reason: query_translation_required`;
- 15 попыток `local_library` — `reason: license_review_required`;
- `provider_errors: []`, отправленных query-строк: `[]`.

Сообщение системы честное и точное: «Нет английского запроса для этой сцены:
добавьте provider_queries или английские поля в visual brief. Русский запрос
не отправлен.»

Разрешения `provider_search` и `asset_download` были выданы и **не
использованы ни разу**. Сеть не трогалась.

### BLOCKER-S2 · Детерминированная экстракция даёт бессмысленные запросы

`primary_query`, построенный из русского сценария без брифа:

| Сцена | Текст сценария | Полученный запрос |
|---|---|---|
| 1 | Одна рабочая пчела за день облетает несколько тысяч цветков | `Одна пчела цветка` |
| 2 | Пыльцу она переносит на задних ногах — в крошечных корзиночках из щетинок | `Пыльцу переносит задних` |
| 3 | Так опыляется большинство цветковых растений вокруг нас | `большинство опыляется вокруг` |
| 4 | Около трёх четвертей главных пищевых культур в той или иной мере зависят от опылителей | `главных зависят иной` |
| 5 | Поэтому урожай целого поля начинается с одного цветка | `Одна начинается цветка` |

Субъектами выбраны грамматические дополнения и служебные слова. `главных
зависят иной` не описывает ничего. Это воспроизводит дефект, уже записанный в
плане при PLAN-9D-C, на новом материале и на текущем HEAD.

Закрывающая capability — `semantic_brief` — существует
(PLAN-9B-PRODUCER-M), но требует отдельного network action и обращения к
текстовой модели; владелец явно оставил paid semantic **OFF**, поэтому она не
включалась.

### Требования владельца к отчёту RUN 2

1. **Путь к MP4** — файла нет. `final_video=нет готового файла`.
2. **Запрос → провайдер → asset по сценам** — таблица выше; провайдер ни разу
   не выбран, asset ни разу не выбран.
3. **Semantic match и качество кадров** — неприменимо: кадров нет.
4. **Abstain/block** — `completion.slot_count: 0`,
   `scenes_usable_in_draft: 0`, `timeline_problems: assembly_has_no_slots` ×5,
   `weak_fragment_count: 5`, все quality tiers `A_exact…F_emergency` — нули.
   Replacement report сформирован и лежит в
   `replacement/replacement_report.json`.
5. **Ручные действия** — выбор темы и написание сценария. Больше ничего:
   прогон остановился раньше, чем потребовалось вмешательство.

---

## Сравнение LOCAL vs STOCK

| Критерий | LOCAL (RUN 1) | STOCK (RUN 2) |
|---|---|---|
| Semantic match | н/д — материал отклонён по правам | н/д — запросы не отправлены |
| Качество исходников | высокое (Pexels HD, проверено глазами) | неизвестно |
| Vertical crop | проверен, все 5 клипов проходят 9:16 | неизвестно |
| Narration / timing | не достигнуто | не достигнуто |
| Subtitles | не достигнуто | не достигнуто |
| Ручных действий | много: поиск, просмотр, кроп-проверка, бриф, сценарий | мало: тема и сценарий |
| Где abstain/block | rights (`review_required`) → нет слотов | `query_translation_required` → нет запросов → нет слотов |
| Где пришлось вмешиваться | нигде — вмешательство оказалось невозможно | нигде |
| Слабые сцены | все 5 unresolved | все 5 unresolved |
| Удобство | вход отсутствует | вход есть, упирается в язык запроса |

**Какой путь ближе к продукту.** STOCK. У LOCAL нет входа в канонический CLI
вообще — это не «неудобно», а недостижимо, и починка требует нового contract
(способ передать локальные файлы в `fullscreen_voiceover_v1`). У STOCK путь
существует целиком и упирается в одно недостающее звено — английский запрос
для провайдера. Закрыть одно звено дешевле, чем построить отсутствующий вход.

## Что НЕ делалось

Код не менялся. `media_index.json` не правился. Права не ослаблялись.
Манифесты вручную не редактировались. Новых PLAN-ID не создано. `PLAN-9D`
остался текущим checkpoint. M1-E / VA-NEW-09 не начинался.
