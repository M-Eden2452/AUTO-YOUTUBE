---
status: proposal
audit_date: 2026-08-17
audit_head: ca66f59f4f53c03ded818d29ff3ba3231d074b41
working_branch: governance-reset
scope: >-
  исполнимый план внедрения находок 15–17.08 по пакетам: что перепроверено
  по коду перед планом, состав каждого пакета, файлы, приёмка, класс риска,
  нужные решения, и готовый промпт первой сессии
---

> **PROPOSAL.** Это план, а не разрешение на действие. Каждый пакет становится
> работой в своей сессии по правилам [AGENTS.md](../../AGENTS.md); строки
> реестра, изменения плана, ADR и `PRODUCT_PLAN.md` вносятся **той** сессией, а
> не этим документом. Что и почему сводится — в
> [FINDINGS_CONSOLIDATION_AND_ROLLOUT_2026-08-17.md](FINDINGS_CONSOLIDATION_AND_ROLLOUT_2026-08-17.md)
> (далее — «сводка»). Индекс — [README.md](README.md).

# План внедрения находок 15–17.08 — 2026-08-17

## 0. Что перепроверено по коду перед планом

План опирается только на утверждения, которые держатся на `ca66f59` **сейчас**.
Перепроверены несущие — те, от которых зависит состав или порядок пакетов.

### 0.1. Подтверждено

| Утверждение | Источник | Проверка |
|---|---|---|
| 7 отчётов не в git, 7 коммитов не запушены, 4 файла не в индексе | сводка §2 | `git status`, `git log origin..HEAD` (7, **не 8** — сводка исправлена) |
| `next_exact_action` — 142 строки, 10 680 символов; guard'а на длину в чекере нет | опись 7 | `awk` по frontmatter; `check_agent_docs.py:828-836` проверяет только ссылки |
| ADR остановлены на `0016` (2026-07-29); `AGENTS.md:13` ссылается на `docs/adr/` | сводка §5 | `ls docs/adr/`, `grep -i adr AGENTS.md` |
| `validate_adr_references` уронит чекер на ссылке на несуществующий ADR из strict-документа | память | `check_agent_docs.py:1287-1307` |
| `REGISTRY_ROW_MAX_LENGTH = 1500` | сверка | `tests/test_stage2_agent_onboarding.py:44` |
| 137 модулей `test_*.py` / **48 852** строки; 144 файла `*.py` / 53 495 | опись 5, аудит тестов | `wc -l` |
| `script_mismatch`/`is_undecidable` — **0** упоминаний в `tests/` | аудит тестов §7.1 | `grep -rl` |
| 12 модулей замораживают реализацию через `inspect.signature`/`getsource` | аудит тестов §6 | `grep -rl` |
| В «Accidental invariants» только `test_legacy_pipeline_internals_contract`; ещё 4 `*_internals_contract` существуют | аудит тестов §8 | `awk` по разделу; `ls tests/` |
| Пять точек вызова примитива: `decision.py:310,363,378`, `candidate_ranker.py:345` (прямой `_script_mismatch`), `:633` (`is_undecidable`) | сверка §7 | чтение |
| `K1`: `if not has_evidence or must_undecidable: reject_reasons.append("semantic_unverified:…")` — русский `must_include` блокирует английский пул | языковой §4A | `candidate_ranker.py:344-347, 418-420` |
| `K3`: `_score_asset` читает `keywords`/`mood`/`channel_tags`/`type`/`scene_tags`, **не** `title`/`description` | языковой §4A | `media_library.py:536-568` |
| `K4`: `create_default_stock_providers` не создаёт `LocalLibraryStockProvider`; единственный конструктор — в диагностике с пустым индексом | языковой §4A | `providers/registry.py:56-79`, `provider_diagnostics.py:125` |
| `K5`: `relevance_score`/`total_score` локального матчера не читаются ни одним модулем `semantic_selection/` | языковой §4A | `grep` по каталогу — пусто |
| `K6`: `min_score=1` | языковой §4A | `asset_manifest_builder.py:1612` |
| `K7`: `_mentions_avoided` токенизирует `_PROVIDER_TOKEN_RE = [A-Za-z]…` — кириллический `must_avoid` даёт пустой `wanted` и никогда не совпадает | языковой §4A | `visual_planning/expansion.py:54, 270-286` |
| `K12`: корпус PLAN-9D — 14 сцен, **14/14 английских субъектов**, 1 064 кандидата, кириллица в 3 из них, `media_index` в сценах отсутствует | языковой §4.15 | скрипт по `tests/data/plan9d/current_corpus_v1.json` |
| Anime Factory: 344 сегмента, 30 кандидатов, 90 превью, **0** в `output/`, 0 crops; заморожен с `13cc3f4` 2026-07-25 | опросник §0.2, §0.6 | `anime_factory/episodes/episode_001/` |
| Библиотека: 85 mp4 + 63 jpg, 72 курированные, 86 в `media_index` | опросник §0.7 | `assets/library/` |
| `cv2`, `soundfile`, `torch` — нет; `faster-whisper 1.2.1`, `ctranslate2 4.7.1` — есть | опросник §0.4 | импорт из `venv` |
| Каналов 6; `allow_unknown_rights` — единственное вхождение в репозитории вне отчётов | опросник §0.8, аудит тестов §7.5 | `ls config/channels/`, `grep -rn` |
| `ElevenLabsProvider.synthesize` делает `self.http.post` без `require_network`; класс `NETWORK_ACTION_VOICE_PREFLIGHT` стоит только на preflight | опись 82 | `elevenlabs_provider.py:33-50, 89, 136` |
| Генератора изображений в каноническом коде нет (единственное совпадение — путь `assets/images/generated` в `asset_finder.py:75`) | опросник §0-B.3 | `grep` |
| Символы аудита тестов существуют: `prepare_candidate_preview_analyses` (`visual_preview.py:343`), `_create_video_preview` (`:675`), `_prepare_visual_review` (`asset_manifest_builder.py:698`) | аудит тестов §3 | `grep` |

### 0.2. Не подтвердилось или изменилось — план это учитывает

| Было написано | Факт | Следствие |
|---|---|---|
| Опись п. 1 «единый статус WP0-B» — ✅ делать | **Уже сделано** `9d6c09f` (12:27, до описи): план `:1083` — «Это единственный статус `WP0-B` в current-документах»; `START_HERE:50` согласован | Пакет B: только проверить, не делать |
| Опись п. 5 «замер тестов `112 / 30 403` устарел» | Заменён тем же `9d6c09f` на **`137 / 53 221`** (`:2311-2313`) — но 53 221 — все `*.py`; для `test_*.py` верно **48 852**; мой `wc` по `*.py` даёт 53 495 | Пакет B: правка одного числа с явной базой |
| Сводка §9 №2: «порог K/M вместо пина — блокирует пакет C» | Опись п. 38 (позже сверки) — **❌ не делать**: пин остаётся, к нему добавлен `measure` | Пакет C **не** заблокирован порогом; сводка исправлена |
| Опросник §0-B.3: «восемь провайдеров ассетов» | Файлов **9**: есть `unsplash_provider.py` — legacy-функция `search_unsplash_images`, не класс реестра | Вывод «генератора нет» держится; число исправлено в сводке |
| Языковой `K12`: «2 кириллические записи из 1064» | Мой счёт — **3** (любая строка кандидата) | Метод счёта разный; вывод держится |
| Аудит продукта §10: «18 кандидатов» | 17 буллетов, часть по 2–3 кандидата | Счёт приблизительный; в сводке — «≈18» |

### 0.3. Не перепроверялось (взято из отчётов как есть)

Замеры времени тестов (178.5 с, 74.2 с, −171 с); `K2` (59 из 273 сцен без
запроса), `K8` (42 из 655 запросов с `shot_type`), `K11` (≥29 из 62 субъектов —
не существительные) — требуют скриптов из временных каталогов сессий; ответы
владельца из опросника — из переписки. На порядок пакетов эти числа не влияют.

---

## 1. Порядок

```
0 ─ A ─ B ─┬─ C ─ D ─ E ─ F …
           ├─ ADR (+P)   параллельно, не на критическом пути
           ├─ T
           └─ G           governance-полоса, когда удобно
```

| Пакет | Сессий | Класс | Блокирует | Решения владельца |
|---|---|---|---|---|
| **0** Сохранность | 0.5 | LOW | всё остальное (иначе нечего внедрять) | — |
| **A** Точка истины `C89` | 0.5 | LOW, read-only | D | — |
| **B** План перестаёт себе противоречить | 0.5–1 | LOW | — | — |
| **ADR** Долговечные решения (+ перенос в `PRODUCT_PLAN`) | 1 | MEDIUM | — | подтвердить состав (§ADR) |
| **T** Тесты | 1 | LOW | — | форма снятия превью |
| **C** Прибор | 1 + время владельца | MEDIUM | D | слепая разметка корпуса v2 |
| **D** Доказуемость | 1 | HIGH | E, F | **вариант A/B**; расширить ли `C91` |
| **E** Безопасность перед платным прогоном | 1 | HIGH | LIVE-6 | — |
| **F** Смысл → движение → читаемость → LIVE-6 → витрина → три темы | по описи §K | — | — | опись 48·54·62·69·95 |
| **G** Governance-полоса: опись 8·9·10·11·13·12 | по мере | LOW | — | — |

Три правила порядка, каждое с причиной:

- **0 впереди A** — A ничего не сохраняет, а шесть отчётов вне git.
- **C строго перед D** — `K12`: замороженный корпус не видит языка, приёмка
  `C91` пройдёт на любой правке. Сначала прибор, который увидит дефект.
- **ADR не ждёт** — три решения владельца уже приняты и существуют только в
  незакоммиченном файле. План `:1093-1096` сам требует ADR-бэкфилл **до**
  архивации закрытых секций — полоса G зависит от ADR, не наоборот.

---

## 2. Пакеты

Формат: цель · вход · шаги · файлы · приёмка · проверки · коммит · чего не делать.

### Пакет 0 · Сохранность

**Цель.** Ни один из отчётов 15–17.08 не может быть потерян; каждый находится
через индекс.

**Вход.** Рабочее дерево: 7 `??` в `docs/audits/`, `M docs/audits/README.md`,
`?? tmp/`, 7 коммитов ahead.

**Шаги.**
1. `docs/audits/README.md` — **шесть** строк индекса:
   `PRODUCT_INTERVIEW_MERGED` (готовая строка — его Приложение 2),
   `LIVE_5_2026-08-15`, `CURATED_LIBRARY_2026-08-14`,
   `FIRST_OWNER_SHORT_2026-08-13`, `FINDINGS_CONSOLIDATION_AND_ROLLOUT`,
   `ROLLOUT_PLAN` (статус `proposal`). Формат — как у соседей: дата · статус ·
   где current truth · о чём.
2. `ALL_PROPOSALS_INVENTORY` — одна строка под шапкой: «продолжено пунктами
   107–142 в сводке 08-17». Больше в описи не менять.
3. Внешние ревью: если владелец приложит исходники GPT-5.6 Sol и Antigravity —
   `docs/audits/external/<источник>_<дата>.md` как есть, с двухстрочной шапкой
   «внешний источник, писался без доступа к коду; проверка — [EXTERNAL_REVIEW_VERIFICATION]».
   Если исходников нет — одна датированная строка «Примечание 2026-08-17» в
   шапке проверки: оригиналы в репозитории не сохранены. Не переписывать текст
   проверки.
4. `tmp/PROMPT_TEST_AUDIT.md` — не трогать и не коммитить; `tmp/` не в
   `.gitignore` — это владельцу: либо в `.gitignore`, либо перенос в
   `docs/audits/` как evidence промпта. В пакет 0 не входит.
5. `.\venv\Scripts\python.exe -m tools.qa.check_agent_docs` → зелёный;
   `.\venv\Scripts\python.exe scripts\gates.py` → `GATES OK`.
6. Один коммит `docs(audits): …` со всеми отчётами и индексом; **push**
   (8 коммитов после этого).

**Файлы.** Только `docs/audits/**`. `docs/current/`, код, тесты, `projects/`,
`tmp/` — не трогаются.

**Приёмка.** `git status` чист в `docs/audits/`; каждый файл каталога имеет
строку в индексе (проверить циклом по `ls`); `origin/governance-reset == HEAD`.

**Коммит.** Как `ca66f59`: прозаическое тело по-русски — что добавлено, что
не тронуто, что чекер и gates зелёные; без `Plan-Step:` (не шаг плана);
трейлер `Co-Authored-By`.

**Чего не делать.** Строк реестра, правок `docs/current/`, ADR, любых
переформулировок внутри отчётов кроме п. 2–3.

### Пакет A · Точка истины `C89`

**Цель.** Независимое ревью двух уже закоммиченных правок отбора (`ec369f8`,
`ede8c4b` — `C79`/`C89` в `evidence.py`, `candidate_ranker.py`) до того, как на
них ляжет третья (пакет D). Сверка §9 прямо записала: «исполненные слайсы
независимо не рецензировались».

**Шаги.** Skill `review-change` в новом контексте на диапазоне
`a7bec3c..ede8c4b` (или двумя вызовами по коммиту). Read-only. Вердикт и
findings — в файл `docs/audits/REVIEW_C79_C89_<дата>.md` + строка индекса.
Findings класса BLOCKER/MAJOR — тестами в пакете D до правки 44, не отдельным
слайсом.

**Приёмка.** Вердикт назван; ноль изменений кода; `C79`/`C89` в реестре получают
датированную приписку «reviewed <дата>, verdict …».

### Пакет B · План перестаёт себе противоречить

**Цель.** `next_exact_action` снова указатель, а не журнал; долг ADR записан;
числа плана верны.

**Шаги.**
1. `next_exact_action` → **≤ 600 символов**: checkpoint (`PLAN-9D`) · следующее
   действие · условие · кто решает. Чекер (`check_agent_docs.py:828-836`)
   требует, чтобы поле называло `current_checkpoint` и только определённые
   PLAN-ID — сохранить. Журнал «IS DONE / closed by» **не удаляется**, а
   переезжает одним блоком в существующий раздел «Current checkpoint» плана
   (или в `docs/archive/execution_history/`, если владелец решит запускать
   опись 8 тем же слайсом — не рекомендуется, это отдельная полоса G).
2. Guard в том же коммите: `NEXT_EXACT_ACTION_MAX_CHARS = 600` рядом с
   существующей проверкой в `check_agent_docs.py`; тест — в существующем
   владельце `tests/test_check_agent_docs.py`. Правило волны диеты из
   ревизии 2.2: замок и уборка — одним коммитом.
3. Строка реестра: «долг ADR-бэкфилла (R9) нигде не записан; утверждение
   внешнего ревью о его фиксации ложно» — класс FACT, existing owner —
   `docs/adr/` + блок WP0-B плана `:1083`. ≤ 1 500 символов.
4. План `:2311-2313`: добавить «48 852 строки в 137 `test_*.py`» рядом с
   «53 221 (все `*.py`)», чтобы база числа была названа.
5. Проверить, что WP0-B действительно один статус (`grep -n WP0-B` по
   `docs/current/`) — если да, ничего не делать.
6. Блок «Маршрут после 2026-08-17» в плане — **≤ 30 строк**, ссылка на этот
   документ, порядок пакетов, без новых PLAN-ID (их выдаёт владелец при входе в
   пакет). `updated_at` плана.

**Приёмка.** `check_agent_docs` зелёный с новым guard'ом; RED guard'а показан
до правки (укоротить лимит на пробу или тест на фикстуре); план не вырос.

**Класс.** LOW (docs + один guard). Батч-review не нужен.

### Пакет ADR · Долговечные решения (+P: перенос в `PRODUCT_PLAN`)

**Цель.** Три решения владельца из опросника перестают жить в одном
незакоммиченном файле.

**Состав — на подтверждение владельцу в начале сессии** (это решение № 11
сводки, но по существу оно уже принято — вопрос только «сейчас или позже»):

| Решение | Куда | Почему туда |
|---|---|---|
| `Q91` правовая модель репурпозера: любой контент, личное использование, права не проверяются | **ADR 0017** | меняет rights-контракт второго engine; класс HIGH; `publish_ready` для repurposed — никогда `true`; режим виден в манифесте |
| Принцип переключателей per-stage: бесплатно/платно · вручную/авто · проверка/доверие, один раз в шаблоне канала, три положения | **ADR 0018** | архитектурная граница: расширение human review boundary до per-stage; **не** третий completion ladder (§12 `PRODUCT_PLAN`) |
| `Q22` Content Creator первым | `PRODUCT_PLAN` §4 как owner-approved decision | приоритет, не граница — ADR README требует «контракт или граница» |

Формат ADR — как `0016`: `Date`, `Status`, `Context`, `Decision`, `Consequences`,
`Verification`; язык — как у соседей (0016 на английском). **Порядок внутри
сессии:** сначала файлы ADR, потом ссылки из `docs/current/` — иначе
`validate_adr_references` уронит чекер.

**P — перенос в `PRODUCT_PLAN.md`** (та же сессия или следующая, MEDIUM,
батч-review на границе): §2 vision через человека и границу ответственности;
§4 `Q22`; §5/§10 определение v1 — 7 критериев CC (порог **≥ 10/14** при нуле
«выбрал вычеркнутое») + 5 репурпозера; §6 обложка как переключатель
(`Q176`); §7 Hook/Clarity `COMMITTED_LATER` (`Q34`), длительность по нише
(`Q78`); §12 Raw Footage Editor / Channel Producer / Moment Intelligence
Engine — не строить; §17 — счёт 15, закрыть отвеченные. Каждый перенос
помечается «ответ владельца 2026-08-16, источник — опросник».

**Не входит.** R9-бэкфилл шести ADR из аудита 08-12 — отдельно, по блоку WP0-B
плана. `Q31` (пересмотр PD-1) — governance-решение, не оформляется без
отдельного анализа.

### Пакет T · Тесты

**Цель.** Suite дешевле без изменения того, что он проверяет.

**Шаги.** S1 — один модуль `test_input_query_truth_characterization`
(178.5 с): снять материализацию превью там, где она не предмет теста.
Форма — **решение владельца** (аудит §12.1): настройка `visual_preview.enabled`
в конфиге теста **или** seam на `prepare_candidate_preview_analyses`.
Рекомендация — конфиг, если он читается тем путём (`visual_preview.py:851`
держит `"enabled": True`); seam — только если конфиг до теста не доходит.
Затем 118 (ещё три модуля), 120 (+4 модуля в «Accidental invariants» с ценой
в секундах), 121 (пять правок чисел).

**Приёмка.** Assert'ы не менялись (diff теста показывает только setup),
0 failures, модуль ≤ ~70 с, gates зелёные. Одна команда:
`.\venv\Scripts\python.exe -B -m unittest tests.test_input_query_truth_characterization`
с `PYTHONIOENCODING=utf-8`.

**Чего не делать.** pytest, маркеры, тиры каталогами, карта импортов, вынос
`plan9d_current_capture` без решения владельца (аудит §12.2).

### Пакет C · Прибор

**Цель.** Стенд способен увидеть дефект, который чинит пакет D.

**Шаги.**
1. Опись 39 — два класса корпуса (слепая разметка · разбор инцидента) полем в
   схеме `plan9d-corpus-1` без ломки существующего корпуса; harness принимает
   оба.
2. Опись 40 — корпус v2 обязан содержать двуязычные записи и русские словоформы;
   схема и harness готовятся агентом, **наполнение — владелец** (слепая
   разметка кандидатов LIVE-4 / LOCAL after fix / LIVE-5; сверка §7: агент
   выдать не может).
3. Опись 43 — `measure --baseline <json>` печатает изменившихся победителей
   поимённо («сцена 7: был X, стал Y»). Baseline на `41ac1c2` уже записан
   текстом в сверке §7.
4. 114 (`K9`) — отброшенный по языку запрос оставляет след в `query_plan` /
   ledger. Это код `visual_planning`, MEDIUM; без него прибор не видит, что
   именно потерялось.
5. 116 (`K12`) — датированная приписка к gate строки `C91`: приёмка на
   корпусе v1 язык не проверяет; для приёмки D нужен корпус v2.

**Приёмка.** `measure` на v1 даёт прежние 4/14 (регрессии нет); `measure` на
v2-фикстуре с двумя языками показывает хотя бы один случай, где кандидат с
русским полем участвует в исходе.

**Решения.** Слепая разметка — время владельца; порог K/M **не нужен**
(опись 38 ❌).

### Пакет D · Доказуемость

**Цель.** Число сдвигается впервые.

**Шаги, строго в этом порядке.**
1. **117** — characterization-тест, именующий `script_mismatch` и
   `is_undecidable`, на смешанном письме: фиксирует **текущее** поведение
   (7.5 при 100 по `keywords`), краснеет при правке. Владелец — существующие
   тесты `evidence.py` (`tests/test_metadata_evidence_repair.py`).
2. Правка 44 + 107 (`K1`) — по **решению владельца A или B**:
   - **A** (рекомендация): `is_undecidable` пофайловый —
     `all(script_mismatch(concept, f) for f in fields)`; один примитив, чинит
     все пять точек вызова, включая слот-слой запретов; **шире `C91`** →
     решение № 4: расширить формулировку `C91` на слот-слой до начала.
   - **B**: отдельный вопрос только для ранжирования — **запрещён** строкой
     `C91` («второй примитив доказуемости»); выбирается только если владелец
     снимает запрет явно.
3. `measure` на v1 — ноль изменившихся победителей (аддитивность); на v2 —
   сдвиг назван поимённо.
4. Приписка к `C91`: исправлено, коммит, числа до/после.
5. **Независимый `review-change`** после (HIGH по `AGENTS.md`); findings —
   тестами.

**Класс.** HIGH (отбор + права). Owner decision до, ревью после.

### Пакет E · Безопасность перед платным прогоном

Опись 82 — `synthesize` под `require_network` с именованным классом (сейчас
класс есть только у preflight, `elevenlabs_provider.py:89,136`); опись 83 —
`C87` budget guard при нулевой оценке стоимости. Оба HIGH (деньги/сеть):
решение владельца об имени класса — до; тест — что offline `synthesize`
падает с `runtime_network` отказом, а не доходит до `http.post`.

### Пакет F и полоса G

По описи §K: 45·48·49·50·51 → 55·57·58·59 → 62·64·65·67·68·69 → LIVE-6
(платно, отдельное разрешение) → 70–81 → три темы. Плюс из дельты: 108
(`K2`), 109 (`K4`), 110–111 (`K5`/`K6`), 112 (`K7` — HIGH, `must_avoid`),
113, 115 — распределяются по тем же пакетам смысла. Полоса G: 8·9·10·11·13·12,
после ADR-бэкфилла (план `:1093`).

---

## 3. Что каждая сессия делает одинаково

- Начало: `git status --short --branch`, `git log -5 --oneline`,
  `git diff --stat`; `AGENTS.md`; для bounded slice — не весь план, а
  `current_checkpoint` и раздел пакета здесь.
- Один пакет — одна сессия — один или несколько коммитов; смешивать пакеты
  нельзя.
- Перед коммитом — `scripts\gates.py`; `--no-verify` запрещён.
- Никакой сети, платных вызовов, render без отдельного разрешения владельца.
- Конец: `git status`, список коммитов, что не сделано и почему, следующий
  пакет.

---

## 4. Промпт первой сессии — пакет 0

Скопировать целиком в новый чат. Если исходники внешних ревью есть — приложить
файлами к этому же сообщению.

```text
Работай в G:\Projects\AI-YouTube, ветка governance-reset.
Сначала выполни git status --short --branch, git log -5 --oneline и
git diff --stat. Прочитай AGENTS.md, docs/current/START_HERE.md,
docs/audits/README.md, затем docs/audits/ROLLOUT_PLAN_2026-08-17.md
(раздел «Пакет 0») и docs/audits/FINDINGS_CONSOLIDATION_AND_ROLLOUT_2026-08-17.md
(§2, §3, §10). Полностью читать PROJECT_EXECUTION_PLAN.md не нужно: пакет 0 —
docs-only, класс LOW, чекпойнт не двигает.

Выполни только пакет 0 «Сохранность»:
1. В docs/audits/README.md добавь шесть строк индекса в существующем формате
   таблицы: PRODUCT_INTERVIEW_MERGED_2026-08-16 (готовая строка — в его
   Приложении 2), LIVE_5_2026-08-15, CURATED_LIBRARY_2026-08-14,
   FIRST_OWNER_SHORT_2026-08-13, FINDINGS_CONSOLIDATION_AND_ROLLOUT_2026-08-17
   (status current), ROLLOUT_PLAN_2026-08-17 (status proposal). Дату, статус,
   «где current truth» и «о чём» бери из шапки и резюме каждого файла.
   Проверь циклом по ls, что каждый файл каталога упомянут в README.
2. В docs/audits/ALL_PROPOSALS_INVENTORY_2026-08-16.md добавь одну строку под
   шапкой: опись продолжена пунктами 107–142 в сводке 08-17. Больше в описи
   ничего не меняй.
3. Внешние ревью: если я приложил исходники — сохрани их как есть в
   docs/audits/external/ с двухстрочной шапкой «внешний источник, писался без
   доступа к коду; проверка — EXTERNAL_REVIEW_VERIFICATION_2026-08-16.md».
   Если не приложил — добавь одну датированную строку «Примечание 2026-08-17:
   оригиналы обоих ревью в репозитории не сохранены» в шапку
   EXTERNAL_REVIEW_VERIFICATION_2026-08-16.md. Текст проверки не переписывай.
4. Запусти .\venv\Scripts\python.exe -m tools.qa.check_agent_docs и
   .\venv\Scripts\python.exe scripts\gates.py — оба должны быть зелёными.
5. Закоммить всё из docs/audits/ одним коммитом docs(audits): … с прозаическим
   телом по-русски (что добавлено, что не тронуто, чекер и gates зелёные), без
   Plan-Step, с трейлером Co-Authored-By. Затем git push.

Не трогай docs/current/, код, тесты, projects/, tmp/ и содержимое отчётов сверх
пунктов 2–3. Не заводи строк реестра, ADR и не меняй план. Никакой сети,
провайдеров, платных вызовов и render. Если что-то из пакета сделать нельзя —
сделай остальное и назови, что и почему пропущено.

В конце: git status, список коммитов с хешами, подтверждение push, что не
сделано, и следующий пакет по ROLLOUT_PLAN (это пакет A — ревью C89).
```

**Промпт пакета A** — тот же зачин, затем: «Выполни только пакет A: skill
`review-change` на `a7bec3c..ede8c4b`, ничего не меняй, вердикт и findings — в
`docs/audits/REVIEW_C79_C89_<дата>.md` + строка индекса; коммит; в конце —
findings по классам и следующий пакет B».

**Промпт пакета B** — тот же зачин, затем шаги 1–6 пакета B из этого документа
дословно, с указанием: «guard и укорачивание — одним коммитом; RED guard'а
показать до правки».

---

## 5. Ограничения

Перепроверка §0 — точечная по названным `file:line`, не построчная; замеры
времени тестов и три числа языкового аудита не воспроизводились. Ответы
владельца приняты как есть. Ни код, ни конфиги, ни `docs/current/`, ни
`docs/adr/`, ни индекс этим документом не менялись; сеть, платные вызовы,
render и тесты не запускались. HEAD `ca66f59`, ветка `governance-reset`.

---

## 6. Дельта 2026-08-18 — что из отчётов 14–18.08 не записано и не доделано

**Метод.** Проверка выполнена 2026-08-18 от HEAD `6d815c7`, ветка
`governance-reset`. Для каждого из 27 отчётов каталога с датой 14–18.08
проверялись **две разные** вещи: *записан* — находка имеет строку в
[CLEANUP_REGISTRY.md](../current/CLEANUP_REGISTRY.md), раздел в плане, ADR или
строку в [PRODUCT_PLAN.md](../current/PRODUCT_PLAN.md); *внедрён* — существует
коммит, меняющий код или конфигурацию. Статусы строк читались по самим строкам
реестра, а не по индексу каталога. Эта запись ничего не меняет в
`docs/current/`, `docs/adr/` и коде и разрешения на действие не даёт.

**Что уже закрыто и в дельту не входит.** Пакеты **0**, **A**, **B**,
**ADR (+P)**, **T**, **D**, **E** выполнены. Пакет **C** выполнен технически —
прибор считает и дал числа (v1 4/14 → 6/14, v2 2/10), — но шаг PLAN-9D-H не
закрыт: он закрывается полной слепой разметкой владельца.

### 6.1. Не записано вообще — реестровой строки нет ни у одной

Семь находок языкового аудита 16.08 существуют только в описи и в сводке §L.
Для следующей сессии их формально нет.

| # описи | Находка | Класс |
|---|---|---|
| 112 (`K7`) | `mentions_avoided` не видит кириллицу ни с одной стороны — русский `must_avoid` не защищает ролик | **HIGH** |
| 108 (`K2`) | между русской сценой и английскими индексами нет моста: 59 сцен из 273 не отправляют ни одного запроса | MEDIUM |
| 109 (`K4`) | `LocalLibraryStockProvider` не создаётся каноническим реестром; его `query_languages=["en","ru"]` мертво | MEDIUM |
| 110 (`K5`) | `relevance_score` локального матчера не входит в `final_score` (18.0 и 6.0 дают одинаковые 7.5) | MEDIUM |
| 111 (`K6`) | `min_score=1` пропускает 85 видеозаписей из 85 при нулевом смысловом совпадении | MEDIUM |
| 113 (`K8`) | `shot_type` уходит в текст поискового запроса (42 из 655) | LOW |
| 115 (`K11`) | предметом кадра становятся прилагательные, числительные, наречия и инфинитивы (≥29 из 62) | MEDIUM |

Две из них — не бэклог, а **измеренные остаточные причины** расхождения отбора
с разметкой владельца. Разбор
[PLAN_9D_FAILURE_DIAGNOSIS_2026-08-18.md](PLAN_9D_FAILURE_DIAGNOSIS_2026-08-18.md)
делит восемь разошедшихся сцен на четыре класса: потеря предмета темы — 3
(закрыто `C98`), план кадра — 3 (закрыто частично `C99`/`C103`), **неприемлемое
видно в метаданных — 1** (это `K7`), **локальная запись не доходит до порога —
1** (это `K5`/`K6`). То есть после двух исправленных классов остаются ровно эти.

`K7` дополнительно отмечен самой описью: «в реестре нет **ни одной** строки про
`must_avoid`». Состояние `K7` после `C91` и `C97` заново не измерялось —
записать строку нужно вместе с измерением, а не вместо него.

### 6.2. Записано, но не внедрено — открытые строки реестра

Двенадцать строк из отчётов 14–18.08 стоят с невыполненным гейтом.

| Строка | О чём | Гейт / владелец |
|---|---|---|
| `C40` | глобальная локальная стоковая библиотека, конвергенция путей | **PLAN-10D** |
| `C75`, `C76`, `C77`, `C78` | подтверждённые дефекты retrieval-эксперимента, перенесены из LIVE-журнала | **PLAN-10B** |
| `C80` | Vision не имеет своего сетевого класса, маршрут обещает раздельные network и paid | **PLAN-9E** |
| `C82` | форма записи попытки и словарь причин остановки | **PLAN-10A** |
| `C83` | `use_local_library` — мёртвый ключ; строка сужена `250ca9e`, дефект не закрыт | **PLAN-10D** |
| `C87` | USD-guard инертен при нулевой `estimated_cost_per_call_usd` | owner decision, semantic-brief live owner |
| `C90` | `semantic_literal_score` без вызывающих | отдельный cleanup-слайс |
| ~~`C97`~~ | `must_not_include` проверяется двумя путями, дисквалифицирующий не спрашивает доказуемость | **закрыта `1a513b5`**: разделение зафиксировано намеренным, расхождение закреплено тестом |
| `C101` | два мёртвых wrapper'а `asset_manager` мимо `topic_anchor` | решение о patch-points модуля |

### 6.3. Принято, но не доделано

| Пункт | Источник | Фактическое состояние на `6d815c7` |
|---|---|---|
| `S3` быстрый tier вторым job'ом | TEST_SYSTEM_AUDIT §11 | в `.github/workflows/offline-tests.yml` один job; `1551eda` поднял потолок до 45 мин вместо разделения |
| приложение «параллельный прогон» | TEST_SYSTEM_AUDIT 08-18 | измерено, не внедрено — так и записано в самом отчёте |
| `У2` журнал из плана в архив | ROUTE_AND_GOVERNANCE 08-15 | план **8706 строк**, журнал не архивирован и растёт |
| `У7` устаревшее из `docs/` | там же | все четыре файла на месте: `docs/cleanup_report.md`, `docs/project_explanation.md`, `docs/project_map_and_app_split_plan.md`, `docs/current/PRODUCT_EVIDENCE_GATE.md` |
| `В7` промпт 3–5 строк | там же | шаблон существует (план `:1839`), но это ~29 строк |
| `В8` мост «инцидент → пакет» | там же | в Measurement policy отсутствует |
| `В1` сборка `SEL-1` | там же | пакета нет; работа идёт строками `C98`/`C99` |
| `В6` формат закрытия ≤10 строк | там же | журнальные записи PLAN-9D остаются длинными |
| `В10` субтитры по умолчанию | там же, п. 62 описи | не сделано; полоса F |
| `В11` пороги LIVE как стандарт | там же | не записаны в Measurement policy |
| полоса **F** | ROLLOUT §2 | не начата формально; `C98`/`C99`/`C103` — её первая полоса де-факто, но пришли из разбора корпуса, а не из описи |
| полоса **G** | ROLLOUT §2 | не начата; шесть открытых решений владельца |

### 6.4. Документы без адресата

| Документ | Состояние |
|---|---|
| [PROJECT_EXECUTION_PLAN_REVISION_2_2_PROPOSAL_2026-08-15.md](PROJECT_EXECUTION_PLAN_REVISION_2_2_PROPOSAL_2026-08-15.md) | **ноль ссылок** из `docs/current/`; не принят и не отклонён |
| [PRODUCT_ROUTE_PROPOSAL_2026-08-16.md](PRODUCT_ROUTE_PROPOSAL_2026-08-16.md) | **ноль ссылок** из `docs/current/`; маршрут P0–P6 де-факто заменён этим документом, но `superseded` не проставлен |
| `Q31` (пересмотр `PD-1`), `Q189`, вторая половина `Q100` | из PRODUCT_INTERVIEW_MERGED не перенесены намеренно; `Q31` — governance-решение класса HIGH |

Все 27 отчётов при этом **проиндексированы** в [README.md](README.md) с
указателем на current truth — дисциплина индекса не нарушена. Пробел не в
индексе, а в §6.1: находка, у которой нет строки реестра, не становится работой.

### 6.5. Предлагаемый порядок остатка

Порядок предлагается, а не назначается: решение владельца — по каждой строке
отдельно, как и во всём этом документе.

1. **Записать дельту §6.1 строками реестра** (LOW, docs-only, одна сессия). — **ВЫПОЛНЕНО `741885f`**: `C104`–`C110`.
   Причина порядка: пока семи находок нет в реестре, любой следующий порядок
   работ опирается на память одного чата, а не на репозиторий. Строка `K7`
   заводится вместе с измерением её текущего состояния после `C91`/`C97` —
   строка без числа повторила бы ошибку, которую уже поймала `C95`.
2. **`K5`/`K6` + пп. 49–50 описи** — локальный матчер и порог (MEDIUM, офлайн). — **ВЫПОЛНЕНО** `0115816`, `6be3d35`, `41632ca`, `ee253b9`: `C107` закрыта owner decision, `C108` исправлена, п. 49 внутри неё, п. 50 — дедуп; коррекции `C111`–`C114`. Повторно не реализуется.
   Причина: это одна из двух измеренных остаточных причин расхождения, она
   проверяется тем же прибором и тем же `measure --baseline` на обоих корпусах,
   без сети и без денег. `C40`/`C83` — соседи по владельцу, но их гейт
   **PLAN-10D** шире этого слайса и им не открывается.
3. **`K7`** — кириллица в `must_avoid` (**HIGH**: запреты и права). — **ВЫПОЛНЕНО** `96c2deb`, `1a513b5`, `1945437`: `C104` закрыта, `C115`/`C116` записаны. Owner
   decision до работы, независимый `review-change` после, findings — тестами.
   Причина, по которой это третье, а не первое: без §6.1 у слайса нет строки,
   а сам он трогает несущий путь запрета рядом с открытой `C97` — их
   соотношение владелец решает один раз, до правки.

Дешёвая гигиена `У2` и `У7` может ехать любым из этих слайсов как отдельный
коммит: она не блокирует ничего, но снижает стоимость каждой следующей сессии.
Полоса **F** после этого продолжается с пп. 51 · 55 · 57 · 58 · 59 по уже
записанному в §2 порядку.

### 6.6. Состояние §6.5 на `1945437`

Все три предложенных слайса выполнены; §6.5 выше сохранена как написана, статус
проставлен строками. Читать §6.5 как список открытой работы больше нельзя.

Перепроверено 2026-08-18 от HEAD `1945437` прямым замером, а не чтением:
`blind agreement` v1 **6/14**, v2 **2/10** тем же `measure`; девять коммитов
отправлены в `origin/governance-reset`.

Одно число из этих слайсов исправлено строкой `C117`: «187 из 280 сцен не
получают ни одного запроса — это `C105`» верно числом и неверно причиной.
Из 187 молчащих **114** не несут блока `semantic` (планы формата до Q2), **73**
кириллические (`C105`), и все 187 несут непустой `primary_query`; прод-адаптер
на тех же планах молчит на **59** сценах — это и есть охват `C105`.

Что действительно осталось из этого документа: гигиена `У2`/`У7`, полоса **F**
с пп. 51 · 55 · 57 · 58 · 59, полоса **G**, и открытые строки §6.2 без `C97` —
`C40`, `C75`–`C78`, `C80`, `C82`, `C83`, `C87`, `C90`, `C101`, плюс заведённые
этой дельтой `C105`, `C106`, `C109`, `C110`, `C112`. Следующий шаг выбирается
владельцем от этого состояния, а не от порядка §6.5.
